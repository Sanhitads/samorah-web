# Analytics UI baselines

Reference screenshots of the **Executive Dashboard** (`/admin/analytics` → Executive Summary strip),
captured at the completion of **Milestone 2 · Stage 2** for visual-regression comparison in later stages.

| File | Viewport |
|---|---|
| `stage2-exec-desktop.png` | 1440 × 1000 |
| `stage2-exec-tablet.png` | 834 × 1112 |
| `stage2-exec-mobile.png` | 390 × 844 |

Captured against local Supabase with seeded orders (never hosted). GA4/Clarity were unconfigured, so the
Visitors/Conversion cards show their honest "GA4 not configured" empty state — that is expected baseline
behaviour, not a defect. Re-capture and diff these after any stage that changes the Executive Dashboard UI.
